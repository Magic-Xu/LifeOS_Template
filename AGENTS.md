# LifeOS 工作规则

## 入口

用户日常只需写入 `00-收件箱` 和 `01-日记`，其他目录由 Codex 维护。涉及 LifeOS 时先读取并执行 [LifeOS Skill](.agents/skills/lifeos/SKILL.md)；上下文路由、记录字段、归档、回收与备份的详细流程由该 Skill 及其引用文件维护。

## 资料与隐私底线

- 已明确长期保存的原始日记、导入文件、照片、账单和报告不得覆盖或删除。收件箱默认精简归纳，完整保存须本人明确要求；仅对不可替代证据或纪念价值提醒一次，不替本人决定。
- 区分事实、感受、解释与 AI 推断，冲突并列；同一事实只保留一处权威记录，跨领域用链接。日期、来源和状态时效按 Skill 的记录规范执行。
- 个人资料及 AI 上下文明文不得提交 Git；仅规则、模板、脚本、Skill、配置说明、`.gitkeep`，以及根目录 `LifeOS-*.zip.age` 加密备份及其校验文件可提交。密码、令牌、完整证件号码和解密密钥不得入库。
- 健康整理服务于时间线、风险信号和就医准备，不替代诊断；财务先校验口径，不自动交易或执行高风险操作。

## 修改与验证

- 整理前说明归档计划、保存级别及源文件处理。已有明确授权即为相应确认，不逐步重问；授权未覆盖的移动、拆分、删除、覆盖、对外发送或不可逆操作先确认。
- 按 Skill 执行回收政策：精简后的源文件保留 30 天，排除 Git 与备份，永久清理前确认。
- 修改后按 Skill 更新受影响的索引、上下文及当月整理报告，运行 `脚本/检查知识库质量.sh`；检查只读，错误修复，警告如实报告。系统维护同步最近检查结果。
