# LifeOS 工作规则

## 日常入口

- 用户只需向 `00-收件箱` 和 `01-日记` 写入新内容。
- 其他目录由 Codex 在整理或专项任务中维护。
- 涉及 LifeOS 的任务优先使用仓库 Skill：`.agents/skills/lifeos/SKILL.md`。

## 事实完整性

- 用户已经明确长期保存的原始日记、导入文件、照片、账单和报告不得覆盖或删除。
- 收件箱材料默认精简归纳；只有用户明确要求时才完整保存原文。
- AI 不替用户决定完整保存，仅对具有不可替代证据或纪念价值的材料提醒一次。
- 区分客观事实、用户感受、用户解释和 AI 推断。
- 结构化记录必须保留日期和来源；原文未长期保存时记录来源类型，不创建失效链接。
- 资料冲突时并列呈现，不能擅自选择一个版本。

## 修改流程

- 整理前先展示归档计划、保存级别和源文件处理方式；用户确认后再移动或拆分。
- 默认精简材料的源文件移入 `.trash/待清理-YYYY-MM-DD/`，保留 30 天且不进入 Git 或加密备份；永久清理前确认。
- 同一事实只保留一个权威记录，跨领域使用链接。
- 每次整理在 `99-系统/AI整理报告` 留下报告。
- 删除、覆盖、对外发送和不可逆操作必须明确确认。

## 隐私与 Git

- `00-收件箱`、`01-日记`、`10-人生经历`、`20-人物关系`、`30-健康`、`40-财务`、`50-兴趣与生活`、`90-附件` 和 AI 上下文明文不得提交 Git。
- Git 可以跟踪规则、模板、脚本、Skill、配置说明和 `.gitkeep`。
- 仅根目录 `LifeOS-*.zip.age` 加密备份及其校验文件可以提交。
- 解密密钥、密码、令牌、完整证件号码不得进入仓库。

## 健康与财务

- 健康输出用于梳理时间线、风险信号和就医准备，不替代医生诊断。
- 财务输出先校验数据口径，不自动执行交易或高风险操作。
